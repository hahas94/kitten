/**
 * reverse_binary.hpp
 * ------------------
 * Description:
 *   Header file containing declarations for the `Reversed Binary Numbers` problem. 
 * */

#ifndef REVERSE_BINARY_HPP
#define REVERSE_BINARY_HPP

#include <string>

int bit_length(unsigned long num);

std::string to_binary(unsigned long num);

unsigned long to_decimal(std::string binary_number);

#endif // REVERSE_BINARY_HPP

// ============== END OF FILE ==============
